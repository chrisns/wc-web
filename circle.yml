machine:
  node:
    version: stable
dependencies:
  cache_directories:
    - ".meteor/local"
  pre:
    - npm config set loglevel warn
    - curl https://install.meteor.com | /bin/sh
    - meteor build --architecture os.linux.x86_64 $CIRCLE_ARTIFACTS